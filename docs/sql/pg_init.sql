-- ============================================================================
-- PostgreSQL pgvector 初始化脚本
-- Docker 容器首次启动时自动执行，启用 vector 扩展
-- ============================================================================

CREATE EXTENSION IF NOT EXISTS vector;

-- 后续可在此添加向量表的预创建等操作
